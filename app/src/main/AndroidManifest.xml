<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.faithconx">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Faithconx"
        tools:targetApi="31">

        <activity
            android:name=".splash.ui.SplashActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".main.ui.MainActivity"
            android:exported="false" />

        <activity
            android:name=".randomusers.RandomUsersActivity"
            android:exported="false" />

        <activity
            android:name=".signup.ui.ActivitySignup"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <activity android:name=".login.ui.ActivityLogin"
            android:windowSoftInputMode="adjustResize"
            />

        <activity
            android:name=".view.ActivityOtpVerification"
            android:exported="false" />

        <activity android:name=".firebasedbpractice.RegisterActivity" />

        <activity
            android:name=".firebasedbpractice.UploadObjectActivity"
            android:exported="false" />

        <activity
            android:name=".firebasedbpractice.HomeActivity"
            android:exported="false" />

        <activity
            android:name=".firebasedbpractice.SigninActivity"
            android:exported="false" />

    </application>

</manifest>